# Secrets and credentials — never commit these
config.json
config.json.bak-*
*.bak-*
jobber-tokens.json
drive-service-account.json

# Deployment registry — host list, never commit (template: deployments.example.json)
deployments.json

# Runtime state — machine-specific
processed_recordings.json
ingest-attempts.json
location-cache.json
client-scopes.json
converted-quotes.json
ui-metrics.json

# Client data / operational state — contains client PII, never commit
job-plans.json
commitments.json
cruz-calendar.json
review-queue.json
voice-profiles.json
expenses.json
receipts.json
recall-index.json
capture-queue.json
leaderboard-archive.json
contacts.json

# Dependencies
node_modules/

# Logs
*.log
